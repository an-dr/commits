# wire protocol

The binary bones web-panel contract and page/core JSON message shapes live
here. Rust owns compatibility fixtures; TypeScript tests consume the same
bytes. Protocol code is shared by the host adapter and component entry point.

