[package]
name = "commits-watcher"
version = "0.1.0"
edition = "2021"

[dependencies]
bus = { path = "../../vendor/bones/core/bus" }
commits-ipc = { path = "../../ipc/rust" }
notify = "8.2"

[dev-dependencies]
tempfile = "3.20"

