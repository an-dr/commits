# commits page

Browser UI bundle hosted in a bones wry panel. Its VS Code API compatibility
shim keeps the existing `an-dr-commits/web` integration boundary available
without coupling browser code to bones-specific message events.

