# Repository scripts

Scripts that operate on the repository or a developer's machine rather than on
one application's artifacts.

A script belongs here when it prepares tooling, removes generated output, or
assembles a distribution across applications. A script that builds a specific
artifact belongs beside that artifact, under its application.
